PORT=5000

# Secret used to sign authentication tokens (JWT).
# For production, replace this with a long random string, e.g. generate one with:
#   node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=change-this-to-a-long-random-secret-before-deploying
JWT_EXPIRES_IN=7d
